# M4b verdict — Reaper co-processing loop + all-owner stems + order lift (2026-08-18)

**Session outcome: the owner production loop went from design to fully live in
one sitting; all seven listen slots are owner-designed VST renders; the owner
returned a positive global verdict and lifted the integration order in
writing.** Accuracy corpus untouched end to end.

## Verbatim owner trail

All owner prose this session is in `drafts/_m4-owner-scores.md` (append-only):
take-3 verdict ("they sound good"), the in-game-test direction, the
layers/rooms idea (logged as music_set_state design input), and the order
lift ("audio order lifted", 2026-08-18).

## Presentation axis status

- Global verdict on the all-owner material: **positive** ("they sound good").
- The 17 per-box scores: **open by owner choice** — the owner moved the
  presentation instrument to **in-game listening** (first in-game session
  becomes the verdict of record). Partial coverage is recorded as exactly
  that; nothing was scored by a machine.
- Listen sheet re-cut for the all-owner material before the verdict
  (`drafts/_m4-listen-sheet.md`: status, metrics table, playback-chain
  provenance note from the room-calibration finding — no EQ correction
  active on the monitors; balance boxes flagged for two-chain cross-check).
- Headroom: the −1 dBFS ceiling held mechanically on every render all
  session (worst final peak −11.82 dBFS, ui_cap; over-1.0 = 0 everywhere).
  The −10 dB sfx **balance** question moves to the in-game listen. No data
  was retuned on taste this session.

## Loop tooling shipped (commits a4ff608 → cfef311)

| piece | facts |
|---|---|
| `harness/reaper_bridge.rb` | file-IPC client for the installed Reaper-MCP Lua server (mutex + atomic command.json + heartbeat; liveness test skips loudly when Reaper is closed, incl. the fresh-lock/dead-server window) |
| `rake reaper:setup` | builds the listen project through the LIVE bridge: 7 named tracks, MIDI items from the same canonical mapping as `rake midi` (`fixture_note_events`), 7 exact staggered regions, disposable ReaSynth per track, render config self-verified via read-back, WAV format verified from REAPER's own serialization (`evaw`); refuses to overwrite `owner_project.rpp`; saves generated `scaffold.rpp` |
| `rake reaper:render SLOT=<id>` | one-slot render → fresh never-overwritten revision under `inbox/runs/<stamp>-<pid>-<slot>/` → 48 k/frame-exact verification → selective import (`only_slots` + explicit `sources`) → auto listen re-render with per-file sha provenance; canonical all-regions settings restored in `ensure` |
| importer growth | `only_slots`/`sources` (requested-missing refuses; unrequested known slots left alone); 19 tests, real WAV bytes |
| hash-domain fix | `Runner.wav_sha256` now hashes the WAV FILE (was: raw sample buffer) after a false determinism alarm — `cmp` proved the "diverged" artifacts byte-identical; engine determinism was never violated (commit e667690) |
| `handoff/audio-v1/` | seven ORIGINAL 24-bit renders + provenance manifest; each original mechanically verified (importer conversion → sha == pinned stem), roles/bands/frames/peak/rms/twin-pins included; LUFS deliberately deferred to the assets pipeline |

Fallbacks taken: none — the `.rpp` text-generation plan (two-reference
sampling) was replaced wholesale by the live bridge (safer: REAPER
serializes everything; nothing fabricated), with `.rpp` sampling retained as
the unused fallback.

## Data pins (all pushed)

| slot | stem sha256 (PCM16 twin) | original sha256 (24-bit) | stem peak/rms dBFS | pin commit |
|---|---|---|---|---|
| mstem_calm_6s | 5562a026e26f… | fbfe224bf4fe… | −19.45 / −31.96 | 9f780ec |
| mstem_combat_6s | 16b84f472189… | ae6e8729d587… | −10.93 / −19.43 | def2cc3 |
| msfx_drone_4s (take 2) | 74175c1650ff… | 465b447afcd3… | −8.65 / −20.47 | a166b32 |
| msfx_stinger_2s | 0b1c8fef89a5… | cbba621d9e98… | −13.99 / −32.02 | 92c4ce6 |
| msfx_swarmpip_4s | 9c7fe0d40022… | 40c6913c8624… | −21.86 / −43.79 | 6cbde5f |
| mui_confirm_200ms | 8b960e8f06d5… | acfd4776056f… | −21.15 / −31.55 | 92ff3c8 |
| mui_ping_1200ms | 948b8b7935b8… | 90a7ee5c09f7… | −15.79 / −30.74 | d68cfe6 |

Every accepted import moved ONLY the replays that fire its cue (per-file sha
provenance printed and archived in the commit messages) — selective-import
correctness observed seven times. Drone take 1 (owner-reported artifact)
retained at `inbox/runs/20260818-204648-9448-msfx_drone_4s/`.

**Final listen sha set (file-domain, printed == disk, reproduced on a fresh
re-render):** cues `da2a62a1`, duck_overlap `ec8303fe`, music `d644b61e`,
churn `7229d606`, spatial `f5d13763`, ui_cap `cf2e0d99`.

**Gate md5 set — UNCHANGED through the entire session** (verified after the
hash-domain fix): `6e52c7cb`, `306f5a41`, `34f9c630`, `22840322`,
`39e7aec5`, `478e5a5c`. Suite at close: 87 runs / 435 assertions green
(1 legitimate skip: bridge test with REAPER closed). Mirror law green after
every import.

## The order lift

Owner, in writing, 2026-08-18: **"audio order lifted"** (full context in the
trail). The PARKING_LOT integration trigger has fired. `PARKING_LOT.md` and
`docs/integration-readiness.md` carry another session's uncommitted edits,
so their PARKED status lines are superseded by the trail + AGENTS.md until
those files can be edited cleanly.

## M5 (next): integration

Per `docs/integration-readiness.md` — assets-lane ingest of
`handoff/audio-v1` (game-two-assets seat, its manifest/LUFS law); game-two
scope-doc update + adapter + boot order + real-device smoke + gate re-pin on
the integration machine + clock-domain anchor measurement + music_set_state
derivation decision (recommendation on file: audio-side, data-driven) +
initial ~6-event cue mapping chosen with the owner. First in-game listen =
presentation verdict of record.
